🤖 Automated Testing with Robot Framework (SE NPRU Blog)
โปรเจกต์นี้คือระบบ ชุดทดสอบอัตโนมัติ (Automated Testing) สำหรับตรวจสอบการทำงานของเว็บไซต์ SE NPRU Blog เพื่อให้มั่นใจว่าระบบสมัครสมาชิก และระบบล็อกอินทำงานได้อย่างถูกต้องแม่นยำ 100% โดยไม่ต้องใช้คนมานั่งคลิกทดสอบเองทุกครั้งที่มีการแก้ไขโค้ด

🌟 ชุดทดสอบนี้ทำอะไรได้บ้าง?
ชุดทดสอบนี้ถูกเขียนขึ้นในรูปแบบ Complete User Journey ซึ่งจำลองพฤติกรรมผู้ใช้จริง ดังนี้:

สมัครสมาชิกอัตโนมัติ: ระบบจะกรอกข้อมูลสมัครสมาชิกให้เอง

ระบบตัดสินใจอัจฉริยะ: * หากชื่อผู้ใช้ยังไม่เคยมีในระบบ -> จะทำการสมัครสมาชิกใหม่จนสำเร็จ

หากชื่อผู้ใช้ซ้ำ (มีอยู่ในฐานข้อมูลแล้ว) -> ระบบจะข้ามไปหน้า Login ให้โดยอัตโนมัติ

ล็อกอินเข้าใช้งาน: กรอก Username และ Password เพื่อเข้าสู่ระบบ

ตรวจสอบความถูกต้อง (Verification):

เช็คว่ามีปุ่ม "Logout" ปรากฏขึ้นจริงหลังเข้าสู่ระบบ
D
เช็คว่าระบบมีการสร้าง Cookies (Token) เก็บไว้ในเบราว์เซอร์จริง เพื่อยืนยันความปลอดภัย

🛠️ สิ่งที่ต้องติดตั้ง (Installation)
เพื่อให้ชุดทดสอบทำงานได้ คุณต้องติดตั้งเครื่องมือพื้นฐาน 3 อย่าง ดังนี้:

1. Python
Robot Framework ทำงานบนภาษา Python

ดาวน์โหลดได้ที่: python.org

ตรวจสอบการติดตั้ง: พิมพ์ python --version ใน Terminal

2. Robot Framework และ Library ที่เกี่ยวข้อง
เปิด Terminal หรือ Command Prompt แล้วพิมพ์คำสั่งนี้:

Bash
pip install robotframework robotframework-seleniumlibrary webdriver-manager
3. Google Chrome
ชุดทดสอบนี้ถูกตั้งค่าให้รันบน Google Chrome เป็นหลัก

🚀 วิธีการรันการทดสอบ (How to Run)
เปิดโปรเจกต์ Frontend และ Backend ของคุณให้ทำงานปกติ (โดยทั่วไปคือ localhost:5173 และ localhost:5000)

เปิด Terminal ในโฟลเดอร์ที่เก็บไฟล์ทดสอบ

พิมพ์คำสั่งรัน:

Bash
robot frontend_test.robot
📊 วิธีการดูผลการทดสอบ
หลังรันเสร็จ ระบบจะสร้างไฟล์รายงานให้โดยอัตโนมัติ 3 ไฟล์:

report.html: สรุปผลภาพรวม (ผ่าน/ไม่ผ่าน) ในรูปแบบกราฟสีสวยงาม

log.html: รายละเอียดเชิงลึกในทุกขั้นตอนการคลิกและการเช็ค Cookies (แนะนำให้ดูไฟล์นี้หากต้องการตรวจสอบข้อมูลที่ละเอียด)

output.xml: ไฟล์ข้อมูลดิบสำหรับนำไปใช้ในระบบอื่น

💡 ทำไมเราต้องทำสิ่งนี้? (Benefit)
ประหยัดเวลา: ใช้เวลาเพียงไม่กี่วินาทีในการทดสอบระบบทั้งหมด

แม่นยำ: ลดความผิดพลาดจากมนุษย์ (Human Error)

ตรวจสอบได้: มีหลักฐานการทดสอบ (Logs) และ Cookies ยืนยันว่าระบบทำงานถูกต้องจริง